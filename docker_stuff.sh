#   to create an image:
# $ docker build -t mfenwickbd/clojarse-server .

#   to run this image, hitting it with http://localhost:3011/parse?text=1 :
# $ docker run -P -dp 3011:3000 mfenwickbd/clojarse-server
